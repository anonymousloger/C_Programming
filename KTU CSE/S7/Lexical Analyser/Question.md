```
Design and implement a lexical analyser using C language. The lexical analyser should ignore spaces,
tabs and new lines. From the given input file, the program should identify the numbers, keywords,
identifiers, special characters and also count the same. The program should also count the total
no.of lines in the input file.
```
